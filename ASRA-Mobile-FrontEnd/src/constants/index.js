import { COLORS, SIZES, FONTS } from "./theme";
import { TABS } from "./tab";
import { FLATFORMS } from "./flatform";
import { HEADER } from "./header";
import { LOCATION, RADIUS } from "./location";
import {
  SLIDER_WIDTH,
  ITEM_WIDTH_HOME,
  HEIGHT_HOME,
  ITEM_WIDTH_DETAIL,
  HEIGHT_DETAIL,
} from "./carousel";
import { FORMAT_DATE } from "./date";
import { CURRENCY_CONVERT, PAYMENT_DEPOSIT, PAYMENT_STATUS } from "./payment";
import { NOTIFICATION_TYPE } from "./notification";
import { TAB_STATUS, TAB_NAME } from "./tab-search";
import { PRICE_TYPE_SORT } from "./price-sort";
import { DOMAIN } from "./domain";
export {
  COLORS,
  SIZES,
  FONTS,
  TABS,
  FLATFORMS,
  HEADER,
  RADIUS,
  LOCATION,
  SLIDER_WIDTH,
  ITEM_WIDTH_HOME,
  HEIGHT_HOME,
  ITEM_WIDTH_DETAIL,
  HEIGHT_DETAIL,
  FORMAT_DATE,
  CURRENCY_CONVERT,
  PAYMENT_DEPOSIT,
  PAYMENT_STATUS,
  NOTIFICATION_TYPE,
  TAB_STATUS,
  TAB_NAME,
  PRICE_TYPE_SORT,
  DOMAIN,
};
